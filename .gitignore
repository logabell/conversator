# Conversator Runtime State
# These directories contain runtime state and should not be committed

# Isolated OpenCode config (runtime copy of agents, session data)
.conversator/opencode/

# Runtime cache and logs
.conversator/cache/
.conversator/state.sqlite
.conversator/state.sqlite-*

# Memory (disposable indexes, can be regenerated)
.conversator/memory/episodic/
.conversator/memory/index.yaml

# Prompts working files (mutable during conversation)
.conversator/prompts/*/working.md

# Plans in progress
.conversator/plans/active/
.conversator/plans/drafts/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment variables (may contain secrets)
.env
.env.local
.env.*.local

# User's own OpenCode (not ours to manage)
# Note: We use .conversator/opencode/ instead to avoid conflicts
# .opencode/ is intentionally NOT ignored - user may have their own setup
